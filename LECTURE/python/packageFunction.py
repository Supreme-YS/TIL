# 매개변수 X, return값 X
def printCoins():
    print('bitcoin')

# 매개변수 X (값을 넘겨받는 부분 즉, () 부분), return값 O
def returnFunc() :
    return '호출한 쪽으로 값이 전달됩니다.'
# 매개변수 O, return값 O
def sayEcho(name) :
    return name + '님, 반갑습니다.'